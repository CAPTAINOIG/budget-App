<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-9ndCyUaIbzAi2FUVXJi0CjmCapSmO7SnpJef0486qhLnuZ2cdeRhO02iuK6FUUVM" crossorigin="anonymous">
</head>
<body class="bg-secondary">
    
    <div class="col-lg-8 mx-auto rounded shadow px-2 bg-secondary mt-5 my-2">
        <% if (message != "") { %>
         <div class="alert alert-danger">message</div>
        <% } %>
        <form action="major" method="post">
        <h1 class=" bg-primary text-center fw-bold">BUDGET APP</h1>
        <input type="text" class="form-control my-2" placeholder="item" name="item" >
        <input type="text" class="form-control my-2" placeholder="price" name="price">
        <input type="text" class="form-control my-2" placeholder="quantity" name="quantity">
        <button class="btn btn-primary text-light w-100 my-2 fs-2 fw-bold text-dark">SUBMIT</button>
    </form>
    </div>


    <div class="container-fluid col-lg-9 mx-auto mt-lg-5">
        <div class="row px-2">
            <% for( let index = 0; index < budget.length; index++ ) { %>
                <div class="col-3 ">
                    <div class="card" style="width: 13rem;">
                        <div class="card-body">
                            <p class="text-dark fw-bold"><%= index+1 %></p>
                            <p class="text-dark fw-bold">ITEM: <%= budget[index].item %></p>
                            <p class="text-dark fw-bold"> PRICE: #<%= budget[index].price %></p>
                            <p class="text-dark fw-bold"> QUANTITY: <%= budget[index].quantity %></p>
                            <p class="text-dark fw-bold"> TOTAL: <%= budget[index].quantity * budget[index].price %></p>
                            <div class="d-flex">
                                <form action="/delete" method="post">
                                    <input type="hidden" value=<%= budget[index].item %> name="newItem">
                                    <button class="btn btn-danger">Delete</button>
                                </form>
                                <form action="/edit" method="post">
                                    <input type="hidden" value=<%= budget[index].item %> name="newItem">
                                    <button class="btn btn-primary ms-3">Edit</button>
                                </form>
                            </div>
                        </div>
                    </div>
                </div>
                <% } %>
        </div>
    </div>


</body>
</html>